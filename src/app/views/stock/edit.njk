{% extends "layout.njk" %}

{% block content %}
    <div class="card show-edit-create-container">
        <div class="card-header">
            <h2>Detalhes</h2>
        </div>
        <form action="/stock?_method=PUT" method="post" class="details">

            {% include "stock/fields.njk" %}

            <input type="hidden" name="id" value="{{ item.id }}">

            <button type="submit">Salvar item</button>
        </form>
    </div>

{% endblock content %}
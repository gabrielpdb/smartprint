{% extends "layout.njk" %}

{% block content %}
    <div class="card show-edit-create-container">
        <div class="card-header">
            <h2>Detalhes</h2>
            <div class="buttons">
                <form action="/kits?_method=DELETE" method="post" id="form-delete">
                    <input type="hidden" name="id" value="{{ kit.id }}">
                    <button type="submit" class="delete-button">Excluir kit</button>
                </form>

            </div>
        </div>
        <form action="/kits?_method=PUT" method="post" class="details">

            {% include "kits/fields.njk" %}

            <input type="hidden" name="id" value="{{ kit.id }}">

            <button type="submit">Salvar kit</button>
        </form>
    </div>

    {% include "confirmDelete.njk" %}

{% endblock content %}